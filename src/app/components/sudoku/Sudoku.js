import React, { Component } from 'react';

class Sudoku extends Component {
  render() {
    return (
      <section className="sudoku wrapper">
        sudoku start
      </section>
    );
  }
}

export default Sudoku;
